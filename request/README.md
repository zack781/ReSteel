request_rec.py
Input:

file path

Output:

Width and height of the minimal bounding rectangle

Plot

mapped_DXF.py

Input:

request_dxf → path to the request shape

material_dxf → existing material DXF

rec → rectangle (x, y, w, h) into which to embed

Output:

Updated DXF with request embedded at the optimal position

